hertz:
  service: "gateway"
  address: ":8080"
  enable_pprof: true
  enable_gzip: true
  enable_access_log: true
  log_level: info
  log_file_name: "log/hertz.log"
  log_max_size: 10
  log_max_age: 3
  log_max_backups: 50
  registry_addr: "consul-service:8500"


mysql:
  # dsn: "root:root@tcp(mysql-service:3306)/gateway?charset=utf8mb4&parseTime=True&loc=Local"
  db_host: "mysql-service"
  db_port: 3306
  db_user: "root"
  db_password: "root"
  db_name: "gateway"

  dsn: "%s:%s@tcp(%s:%d)/%s?charset=utf8mb4&parseTime=True&loc=Local"
  max_idle_conns: 20
  max_open_conns: 100
  conn_max_lifetime: 10

redis:
  address: "redis-service:6379"
  username: ""
  password: ""
  db: 0

open_telemetry:
  endpoint: "otel-collector-service:4317"

health_check:
  addr: ":8090"

security:
  public_routes:
    - "/auth/token"
    - "/auth/verify"
    - "/auth/renew"
    - "/user/register"
    - "/user/login"
    - "/products"
    - "/product/search"
